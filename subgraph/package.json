{
  "name": "windmill-exchange-subgraph",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ windmill-exchange",
    "create-local": "graph create --node http://localhost:8020/ windmill-exchange",
    "remove-local": "graph remove --node http://localhost:8020/ windmill-exchange",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 windmill-exchange"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.96.0",
    "@graphprotocol/graph-ts": "^0.36.0"
  }
}
